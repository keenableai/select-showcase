[project]
name = "select-showcase"
version = "0.1.0"
description = "Selected SELECT reports with their agent trajectories, published to GitHub Pages"
requires-python = ">=3.12"
dependencies = ["fire>=0.7"]

[tool.uv]
package = false
